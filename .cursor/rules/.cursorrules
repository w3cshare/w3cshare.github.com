## 文档输出框架 VitePress 规则

---

### 文档组织规范

- 根目录README.md：作为整个项目的首页入口，应包含项目概述、技术栈介绍、快速开始指南及目录导航
- 子项目README.md：作为该子项目的首页，应包含子项目的具体功能介绍、使用方法及API文档链接

### 文档路径规范

- 根目录下的docs/存放通用技术文档（如开发规范、部署指南等）
- 子项目目录下的docs/仅存放该子项目的入门指南等基础文档
- 组件和模块文档直接存放在对应子项目根目录，以便VitePress直接引用

### 文档命名规范

- 使用kebab-case命名方式（如getting-started.md）
- 核心文档使用统一命名（如README.md、getting-started.md、components.md等）

### 文档内容要求

- 简洁明了：文档内容应简洁明了，避免冗余信息，直接说明用途和使用方法
- 标准格式：所有.md文件必须遵循VitePress的markdown格式规范，正确使用前言(frontmatter)、标题层级和组件引用
- 实例导向：提供完整、可运行的代码示例，配合清晰的使用说明
- 文档更新：当组件或功能更新时，应同步更新相关文档，保持文档与代码的一致性

### VitePress配置规范

- 统一入口：VitePress只在MonoRepo项目根目录配置，子项目不单独配置VitePress
- 文档引用：通过根目录VitePress配置统一引用各子项目下的MD文件，形成完整文档体系
- 避免重复：不创建单独的文档文件夹，直接整合到项目目录中，减少不必要的MD文件重复创建
- 文档合并：当多个文档内容相近或可合并时，应合并为单个文档并移除废弃文档

### 实施建议

- 使用相对路径引用文档和资源，确保在VitePress中正确显示
- 组件文档应与组件代码位于同一目录或邻近目录，便于维护和更新
- 利用VitePress的rewrites功能优化URL结构，提升用户体验
- 定期检查文档完整性和准确性，及时更新过时内容

---

## MonoRepo项目概述与规范

---

### 项目架构

- 核心框架：基于 Lerna + Nx + pnpm + Workspace 构建的单体（Mono）项目
- 项目特点：集成全栈开发能力，统一技术栈和开发规范

### 技术栈

1. **前端**
    - 架构模式：微前端架构
        - 支持 Web Components、SPA 和 MPA 多种应用形态
        - 统一使用 TypeScript 开发
    - UI框架：
        - Ant Design Vue
        - Ant Design React
        - @ant-design/pro-components
        - @ant-design/x（AI组件）
        - @ant-design/web3（区块链组件）

2. **后端**
    - 架构模式：微服务架构
        - 基于 NestJS 框架开发
        - 使用 gRPC 进行服务间通信
        - 通过 Consul 实现服务发现与注册
    - 数据层：
        - TypeORM 和 Sequelize 作为ORM工具
    - 权限管理：
        - RBAC角色权限模型
        - CASL策略权限控制

3. **特色功能**
    - AI能力：基于 LangChain + NestJS 构建的 AI Agent
    - 区块链：集成以太坊相关功能组件和服务

4. **基础设施**
    - 包管理：Verdaccio（私有npm仓库）、pnpm（高效包管理）
    - CI/CD：GitLab + Jenkins 自动化流水线
    - 构建工具：Vite、Webpack、UMI.js、tsup
    - 容器化：Docker Compose 编排与部署

### 文档维护

- 根据开发成果与实践经验，持续更新VitePress文档
- 确保文档与代码同步更新，反映最新的架构设计和使用方法
- 每个子项目应包含完整的使用说明、API文档和示例代码

### 开发规范

- 全栈开发统一使用 TypeScript
- 遵循项目既定的代码风格和提交规范
- 新功能开发需同步更新相关文档

### 功能实现一样，包名不一样，

- 根目录"/lint/eslint-plugin-smart"插件和"/lint/eslint-plugin-smarts"插件功能实现是一样的，只是包名不一样。
- 根目录"/lint/stylelint-config-smart"插件和"/lint/stylelint-config-smarts"插件功能实现是一样的，只是包名不一样。
- 根目录"/lint/prettier-config-smart"插件和"/lint/prettier-config-smarts"插件功能实现是一样的，只是包名不一样。
- 根目录"/lint/commit-smarts"插件和"/lint/commit-smart"插件和"/lint/commitlint-smart"插件和"/lint/commitlint-smarts"插件功能实现是一样的，只是包名不一样。
- 以上四类插件的功能实现是一样的，只是包名不一样，所以每次更新都同步更新一下相关代码和文档

---


pip install 时必须带上 `-i https://pypi.tuna.tsinghua.edu.cn/simple`

不要自行修改版本号
